package client;

// a class for wrapping doubles so they can be changed by BuyStockBox.buyStock()
public class DoubleWrapper {
    double val;

    DoubleWrapper(double val) {
        this.val = val;
    }
}
